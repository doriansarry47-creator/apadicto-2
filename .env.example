DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require
SESSION_SECRET=your-secret-key-here
NODE_ENV=production

# Optional: Custom session name (defaults to 'apaddicto.sid')
SESSION_NAME=apaddicto.sid

# Optional: Cookie domain for production (leave empty for development)
COOKIE_DOMAIN=

# Optional: Frontend URL for CORS (defaults to localhost in development)
FRONTEND_URL=http://localhost:5173

# Optional: Vercel deployment URL (automatically set by Vercel)
VERCEL_URL=

